<!-- Modal -->
<div class="modal fade" id="delete-modal_{{ artist.id }}" tabindex="-1" aria-labelledby="deleteArtistModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteArtistModalLabel">Confirm Deletion</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete this artist? This action cannot be undone.
            </div>
            <div class="modal-footer">
                {{ include('dashboard/artists/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
