<div class="container mt-3">
    <div class="d-flex justify-content-between ">
        <a href="{{ path('app_artists') }}" class="text-dark fs-2 text-decoration-none custom-link">artists</a>
        <a href="#" class="mx-auto text-dark fs-2 text-decoration-none custom-link">artwork</a>
        {% if eventsAvailable is defined and eventsAvailable %}
        <a href="{{ path('app_events') }}" class="text-dark fs-2 text-decoration-none custom-link">events</a>
        {% endif %}
    </div>
</div>
